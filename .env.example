# Copy to .env and fill in. NEVER commit the real .env.
# --- Dynatrace trial tenant ---
DT_ENV_URL=https://CHANGEME.live.dynatrace.com
DT_API_TOKEN=dt0c01.CHANGEME            # scopes: settings.read settings.write ReadConfig WriteConfig DataExport entities.read

# --- GitHub (for the submission handler to open PRs) ---
GITHUB_TOKEN=ghp_CHANGEME               # fine-grained PAT with Contents + Pull requests write on the repo
GITHUB_REPOSITORY=youruser/lpl-oaac-poc

# --- Claude Code (headless auth on EC2) ---
ANTHROPIC_API_KEY=sk-ant-CHANGEME

# --- Local ports ---
PORT=8080
SAMPLE_PORT=3000
